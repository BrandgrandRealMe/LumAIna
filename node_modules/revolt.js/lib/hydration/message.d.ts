import { ReactiveMap } from "@solid-primitives/map";
import { ReactiveSet } from "@solid-primitives/set";
import { Message as ApiMessage, Embed, File, Interactions, Masquerade, SystemMessage } from "revolt-api";
import type { Merge } from "../lib/merge";
import { Hydrate } from ".";
export type HydratedMessage = {
    id: string;
    nonce?: string;
    channelId: string;
    authorId?: string;
    content?: string;
    systemMessage?: SystemMessage;
    attachments?: File[];
    editedAt?: Date;
    embeds?: Embed[];
    mentionIds?: string[];
    replyIds?: string[];
    reactions?: ReactiveMap<string, ReactiveSet<string>>;
    interactions?: Interactions;
    masquerade?: Masquerade;
};
export declare const messageHydration: Hydrate<Merge<ApiMessage>, HydratedMessage>;
//# sourceMappingURL=message.d.ts.map