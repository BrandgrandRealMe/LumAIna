"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.EmojiCollection = void 0;
const __1 = require("..");
const _1 = require(".");
/**
 * Collection of Emoji
 */
class EmojiCollection extends _1.ClassCollection {
    /**
     * Fetch emoji by ID
     * @param id Id
     * @returns Emoji
     */
    fetch(id) {
        return __awaiter(this, void 0, void 0, function* () {
            const emoji = this.get(id);
            if (emoji)
                return emoji;
            const data = yield this.client.api.get(`/custom/emoji/${id}`);
            return this.getOrCreate(data._id, data);
        });
    }
    /**
     * Get or create
     * @param id Id
     * @param data Data
     * @param isNew Whether this object is new
     */
    getOrCreate(id, data, isNew = false) {
        if (this.has(id)) {
            return this.get(id);
        }
        else {
            const instance = new __1.Emoji(this, id);
            this.create(id, "emoji", instance, this.client, data);
            isNew && this.client.emit("emojiCreate", instance);
            return instance;
        }
    }
    /**
     * Get or return partial
     * @param id Id
     */
    getOrPartial(id) {
        if (this.has(id)) {
            return this.get(id);
        }
        else if (this.client.options.partials) {
            const instance = new __1.Emoji(this, id);
            this.create(id, "emoji", instance, this.client, {
                id,
            });
            return instance;
        }
    }
}
exports.EmojiCollection = EmojiCollection;
